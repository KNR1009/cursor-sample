/**
 * @file ユーザー一覧ページのルートコンポーネント
 * @description /users ルートのエントリーポイント
 */
import { UsersContainer } from './_container/UsersContainer';

export default function UsersPage() {
  return <UsersContainer />;
}
