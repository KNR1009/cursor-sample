/**
 * @file 投稿一覧ページのルートコンポーネント
 * @description /posts ルートのエントリーポイント
 */
import { PostsContainer } from './_container/PostsContainer';

export default function PostsPage() {
  return <PostsContainer />;
}
